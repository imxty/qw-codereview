name: 'Qianwen Code Review'
description: 'Automated code review using Alibaba Qianwen (通义千问) API'
author: 'Your Name'
inputs:
  qianwen-api-key:
    description: 'API key for Alibaba Qianwen (通义千问)'
    required: true
  qianwen-model:
    description: 'Qianwen model to use (e.g., qwen-turbo, qwen-plus)'
    required: false
    default: 'qwen-turbo'
  github-token:
    description: 'GitHub token with pull-requests: write permission'
    required: true
    default: ${{ github.token }}
  review-comment-title:
    description: 'Title of the review comment'
    required: false
    default: '🤖 千问代码评审意见'
  ignore-comment:
    description: 'Comment pattern to ignore review (default: IGNORE)'
    required: false
    default: 'IGNORE'
  ignored-dirs:
    description: 'Additional ignored directories (comma separated, e.g., docs/,test/)'
    required: false
    default: ''
  batch-size:
    description: 'Number of files to review in one batch (avoid 429, 1-10)'
    required: false
    default: '10'
  max-retries:
    description: 'Max retries for failed requests (1-5)'
    required: false
    default: '3'
runs:
  using: 'node20'
  main: 'dist/index.js'
branding:
  icon: 'code'
  color: 'blue'